import { FunctionComponent } from "react";
import type { Book as BookType, Props } from "../types";
declare const Book: FunctionComponent<{
    book: BookType;
    options?: Props;
}>;
export default Book;
